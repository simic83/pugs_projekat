# Persistence

SQL Server connection handling, DbContext registrations, repositories, and unit-of-work abstractions will be added here when database implementation begins.

DTO models must remain in `TravelPlanner.Contracts`. Entity/database models belong in each service's `Data/Entities` folder.

